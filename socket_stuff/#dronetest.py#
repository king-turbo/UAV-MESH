

from dronekit import connect, VehicleMode, LocationGlobalRelative, Vehicle
import time
import json

#vehicle = connect('/dev/ttyACM0', wait_ready=True) #change to /dev/ttyAMA0 when in serial




#print(vehicle.location.global_frame)



class Drone(Vehicle):
    
    def __init__(self):
        Vehicle.__init__(self)
        self.connect('/dev/ttyACM0', wait_ready=True) #change to /dev/ttyAMA0 when in serial)


if __name__=="__main__":

    uav = Drone()
    print(uav.location.global_frame)
    

